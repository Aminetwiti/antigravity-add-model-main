# Install & 443 refused: Diagnostic + Surgical fix> Extracted from [FIX_ERROR.md](../../FIX_ERROR.md) (split for discoverability).# FIX_ERROR â€” Antigravity 2.2.1 : `127.0.0.1:443 connection refused`> **Date du fix** : 2026-07-11> **Versions concernÃ©es** : Antigravity 2.2.1, repo `antigravity-add-model-main` v2.1.0+> **Patch de rÃ©fÃ©rence** : `scripts/patch_2_2_1.js`> **Statut** : âœ… corrigÃ© via patch chirurgical + MITM manuel sur 443---## âš ï¸ TL;DRL'erreur `127.0.0.1:443 connection refused` a **deux causes distinctes** quise manifestent ensemble si on n'en corrige qu'une :1. **Cause #1 (corrigÃ©e par `scripts/patch_2_2_1.js`)** : trois modules   `dist/cryptoStore.js`, `dist/customModelStore.js`, `dist/schemaValidator.js`   sont absents de l'asar v2.2.1 dÃ©ployÃ© â†’ le proxy ne dÃ©marre pas.2. **Cause #2 (corrigÃ©e par `scripts/mitm/mitm_443.js`)** : le language server   d'Antigravity fait des appels HTTPS directs Ã  `daily-cloudcode-pa.googleapis.com`,   qui est redirigÃ© par le `hosts` file vers `127.0.0.1:443`. Le MITM doit y   Ã©couter pour terminer le TLS et forwarder vers le proxy HTTP sur 50999.**Les deux fixes sont nÃ©cessaires.** Sans le MITM sur 443, le proxy sur 50999tourne mais le LS continue Ã  Ã©chouer avec `127.0.0.1:443 refused`.---## 1. SymptÃ´meAu lancement d'Antigravity 2.2.1 (aprÃ¨s update depuis 2.1.0), le language server afficheen boucle :```Post "https://daily-cloudcode-pa.googleapis.com/v1internal:loadCodeAssist":dial tcp 127.0.0.1:443: connectex: No connection could be made because thetarget machine actively refused it.```L'IDE s'ouvre mais :- Aucun modÃ¨le custom n'est utilisable (erreur systÃ©matique sur `loadCodeAssist`).- Le port `50999` peut Ã©couter ou non selon la cause racine (cf. TL;DR).- Le port `443` n'Ã©coute que sur `127.0.0.2` (PID `svchost.exe` â€” service Windows  lÃ©gitime, sans rapport) **tant que le MITM n'est pas dÃ©marrÃ©**.---## 2. Environnement affectÃ©| Composant | Valeur observÃ©e ||---|---|| `Antigravity.exe` (ProductVersion) | **2.2.1.0** || `app.asar` (wrapper) | 20 827 867 B, datÃ© 2026-07-10 20:16 || `dist/main.js` (dans l'asar) | **14 554 B** â€” patchÃ© (TLS bypass + `require('../proxy-runner')` intÃ©grÃ©s) || `dist/proxy.js` | âœ“ prÃ©sent || `dist/proxy/translators/*` | âœ“ prÃ©sent || `proxy-runner.js` (Ã  la racine de l'asar) | âœ“ prÃ©sent || `dist/cryptoStore.js` | âŒ **absent** â† cause racine || `dist/customModelStore.js` | âŒ absent || `dist/schemaValidator.js` | âŒ absent || `hosts` file | `127.0.0.1 daily-cloudcode-pa.googleapis.com` (lÃ©gitime) || Port 50999 | **DOIT Ã©couter** (proxy HTTP, dÃ©marrÃ© par `proxy-runner.js`) || Port 443 sur 127.0.0.1 | **DOIT Ã©couter** (MITM TLS, dÃ©marrÃ© par `scripts/mitm/mitm_443.js`) |---## 3. Diagnostic (timeline de la rÃ©solution)1. **L'erreur initiale** (`127.0.0.1:443 refused`) est un **symptÃ´me** : la chaÃ®ne   d'appel aboutit Ã  un port sans listener. Ce n'est pas la cause.2. **Lecture du `main.log`** dans `%APPDATA%\Antigravity\logs\main.log` :   ```   [2026-07-11 12:52:46.567] [error] [PATCH] startProxy failed: Cannot find module '../cryptoStore'   ```   â†’ le proxy ne peut pas se lancer car il lui manque `dist/cryptoStore.js`.3. **Lecture de `src/proxy/modelLoader.ts`** (le caller de `cryptoStore`) :   ```ts   import * as cryptoStore from '../cryptoStore';   ```   â†’ chemin relatif `../cryptoStore` rÃ©solu depuis `dist/proxy/` =   `dist/cryptoStore.js`. **Absent de l'asar** â†’ `MODULE_NOT_FOUND`.4. **Lecture de `src/languageServer.ts`** pour comprendre le fallback :   ```ts   const apiServerUrl = proxyPort     ? `http://localhost:${proxyPort}`     : 'https://generativelanguage.googleapis.com';   ```   â†’ quand `startProxy()` Ã©choue, le LS reÃ§oit les URLs Google **officielles**   en fallback.5. **VÃ©rification du `hosts` file** :   ```   127.0.0.1 daily-cloudcode-pa.googleapis.com   ```   â†’ la rÃ©solution DNS redirige vers `127.0.0.1`. Le LS tente alors le port   `443` (HTTPS par dÃ©faut) â†’ **connection refused** si le MITM n'est pas   dÃ©marrÃ©.### ChaÃ®ne de causalitÃ© complÃ¨te```cryptoStore.js absent de l'asar  â†’ startProxy() throw MODULE_NOT_FOUND    â†’ languageServer.ts fallback sur les URLs Google rÃ©elles      â†’ DNS redirige daily-cloudcode-pa.googleapis.com vers 127.0.0.1        â†’ connection TCP vers 127.0.0.1:443 (HTTPS par dÃ©faut)          â†’ ECONNREFUSED si MITM off, OU forward rÃ©ussi si MITM on            â†’ si forward rÃ©ussi : 200 OK (proxy rÃ©pond pour le LS)            â†’ si MITM off : erreur "Post ... v1internal:loadCodeAssist: dial tcp 127.0.0.1:443"```> **Note importante** : la chaÃ®ne ci-dessus ne dÃ©crit que la **Cause #1**> (proxy ne dÃ©marre pas). Si le proxy dÃ©marre correctement MAIS que le MITM> n'Ã©coute pas sur 443, l'erreur `127.0.0.1:443 refused` se manifeste quand> mÃªme, parce que certains appels HTTPS du language server bypass le proxy.> Cf. Â§10 pour la Cause #2.---## 4. Causes racines (deux causes, pas une)### Cause #1 â€” Trois modules omis du pack v2.2.x**Google a omis trois modules du bundle officiel d'Antigravity 2.2.x** :| Module | OÃ¹ il est requis | ConsÃ©quence de son absence ||---|---|---|| `dist/cryptoStore.js` | `dist/proxy/modelLoader.js` (`require('../cryptoStore')`) | `startProxy` crash â†’ proxy off || `dist/customModelStore.js` | `dist/ipcHandlers.js` | Custom models list vides cÃ´tÃ© UI || `dist/schemaValidator.js` | `dist/proxy/translators/*` | Erreurs de validation runtime |Ces trois modules sont prÃ©sents dans le repo source (`src/`), compilÃ©s dans`dist/` par `npm run build`, mais **omis** lors du repack de l'asar danscertaines itÃ©rations.### Cause #2 â€” MITM sur 443 jamais dÃ©marrÃ© automatiquement**Le language server d'Antigravity fait certains appels HTTPS directs** Ã `daily-cloudcode-pa.googleapis.com` qui **bypassent** le proxy sur 50999.Le `hosts` file redirige ces appels vers `127.0.0.1:443` (HTTPS par dÃ©faut),mais le proxy sur 50999 est en HTTP plain â€” il ne peut pas intercepter duHTTPS. **Le MITM `scripts/mitm/mitm_443.js` doit tourner en parallÃ¨le** pourterminer le TLS et forwarder vers le proxy.> **Erreur d'analyse initiale** : lors du diagnostic du 2026-07-11, j'avais> affirmÃ© Ã  tort Â« le MITM sur 443 n'est pas requis pour le flow actuel Â»> parce que `languageServer.ts` passe `http://localhost:50999` au LS via> `--api_server_url`. En rÃ©alitÃ©, le LS garde des appels HTTPS codÃ©s en dur> pour `daily-cloudcode-pa.googleapis.com` (vu dans le `language_server.log`> : `failed to get load code assist response: Post "https://cloudcode-pa.googleapis.com/...`)> qui ne respectent pas ce flag.### Pourquoi la Cause #1 est apparue (contexte historique)La Cause #1 est liÃ©e Ã  un **changement de structure d'asar entre 2.1.0 et 2.2.1**(cf. session kimchi `019f4c39` du 2026-07-10, message de 14:02:01) :| | Antigravity 2.0.x / 2.1.0 | Antigravity 2.2.x (officiel) ||---|---|---|| `dist/proxy.js` | âœ… prÃ©sent | âŒ **supprimÃ© par Google** || `dist/proxy/translators/*` | âœ… prÃ©sent | âŒ **supprimÃ©** || `proxy-runner.js` (racine) | âœ… | âœ… conservÃ© comme hook || TLS bypass + `require('../proxy-runner')` dans `dist/main.js` | âœ… | âœ… conservÃ© || `cryptoStore.js` / `customModelStore.js` / `schemaValidator.js` | âœ… (compilÃ©s) | âš ï¸ **omis** (probablement par erreur de pack) |L'asar v2.2.1 que Google livre inclut `dist/proxy.js` et les translators(injectÃ©s par notre patch prÃ©cÃ©dent), mais pas les 3 modules dÃ©pendants.---## 5. Le fix (chirurgical)### Pourquoi Â« chirurgical Â» et pas Â« overlay complet Â»Un overlay de `dist/` complet (toute la sortie `npm run build`) **cassel'application** parce qu'il remplace `dist/main.js` :| Fichier | Wrapper v2.2.1 patchÃ© | Repo `dist/` | ConsÃ©quence ||---|---|---|---|| `dist/main.js` | **14 554 B** (TLS bypass + `require('../proxy-runner')` intÃ©grÃ©s) | **17 157 B** (clean) | âŒ patch integration perdue â†’ app crash au boot || `dist/proxy.js` | âœ“ | mon repo | Ã©crasÃ© inutilement || `dist/__mocks__/*` | âŒ | âœ“ (mocks vitest) | âŒ Electron rÃ©sout ces mocks comme modules â†’ crash || `dist/cryptoStore.js` etc. | âŒ | âœ“ | âœ… ajoutÃ© (correct) |**Le fix correct** ajoute **uniquement les 3 fichiers manquants** et laisseintact le `dist/main.js` du wrapper.### Commandes de fix```bashREPO=/mnt/c/Users/amine/Downloads/antigravity-add-model-main/antigravity-add-model-mainRES=/mnt/c/Users/amine/AppData/Local/Programs/Antigravity/resourcesTS=$(date +%Y%m%dT%H%M%S)# 0. Stopper Antigravity + language_serverpowershell.exe -Command "Stop-Process -Name Antigravity,language_server -Force -ErrorAction SilentlyContinue"# 1. S'assurer que dist/ est Ã  jour (sinon les 3 modules manquent)cd "$REPO" && npm run build# 2. Snapshot du asar actuelcp "$RES/app.asar" "$RES/app.asar.pre-fix-$TS.bak"# 3. Patcher (chirurgical, idempotent)NODE_PATH="$REPO/node_modules" node "$REPO/scripts/patch_2_2_1.js" \    "$RES/app.asar" "/tmp/ag-build-$TS" "/tmp/app.asar.fixed-$TS"# 4. VÃ©rifier le delta de taille (~40 KB attendu, >100 KB = suspect)SIZE_BEFORE=$(stat -c %s "$RES/app.asar")SIZE_AFTER=$(stat -c %s "/tmp/app.asar.fixed-$TS")DELTA=$((SIZE_AFTER - SIZE_BEFORE))echo "delta: $DELTA B"[ $DELTA -gt 100000 ] && { echo "ABORT: delta too large"; exit 1; }# 5. DÃ©ployercp "/tmp/app.asar.fixed-$TS" "$RES/app.asar"# 6. Relancer"$RES/../Antigravity.exe" &```### Fichiers effectivement ajoutÃ©s| Fichier | Taille | RÃ´le ||---|---|---|| `dist/cryptoStore.js` | 5 679 B | Encryption API keys (safeStorage) || `dist/cryptoStore.d.ts` | 1 167 B | Types || `dist/cryptoStore.js.map` | 3 201 B | Source map || `dist/cryptoStore.d.ts.map` | 677 B | Source map || `dist/customModelStore.js` | 6 262 B | Persistance des modÃ¨les custom || `dist/customModelStore.d.ts` | 2 448 B | Types || `dist/customModelStore.js.map` | 3 407 B | Source map || `dist/customModelStore.d.ts.map` | 1 548 B | Source map || `dist/schemaValidator.js` | 7 769 B | Validation des rÃ©ponses API || `dist/schemaValidator.d.ts` | 1 731 B | Types || `dist/schemaValidator.js.map` | 6 870 B | Source map || `dist/schemaValidator.d.ts.map` | 771 B | Source map || **TOTAL** | **~41 530 B** | |---## 6. VÃ©rification post-fix### 6.1 Ports```bashpowershell.exe -Command "Get-NetTCPConnection -LocalPort 50999 -State Listen -ErrorAction SilentlyContinue |  Format-Table LocalAddress,LocalPort,OwningProcess,State -AutoSize"```Attendu :```LocalAddress  LocalPort  OwningProcess  State------------  ---------  -------------  -----127.0.0.1     50999      <PID>          Listen```### 6.2 Log```bashgrep -i "startProxy\|cryptoStore\|customModelStore" "$APPDATA/Antigravity/logs/main.log" | tail -5```Attendu :```[â€¦] [info]  [LS] before startProxy[â€¦] [info]  [LS] after startProxy, port: 50999[â€¦] [info]  [Proxy] Loaded custom models count: 5[â€¦] [info]  [Proxy] Custom model "minimax-m3" => slug: custom-minimax-m3 â€¦[â€¦] [info]  [Proxy] Custom model "kimi-k2.7" => slug: custom-kimi-k2-7 â€¦```**Aucune ligne** :```[â€¦] [error] [PATCH] startProxy failed: Cannot find module '../cryptoStore'```### 6.3 Endpoints```bashpowershell.exe -Command "Get-Content '$env:APPDATA\Antigravity\logs\main.log' |  Select-String -Pattern 'Response for /v1internal:' |  Select-Object -Last 5"```Attendu : tous `status: 200`.---## 7. Patcher durable