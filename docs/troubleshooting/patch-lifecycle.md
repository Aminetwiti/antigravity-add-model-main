# Patch lifecycle (Antigravity update -> repatch)> Extracted from [FIX_ERROR.md](../../FIX_ERROR.md) (split for discoverability).`scripts/patch_2_2_1.js` est conÃ§u pour Ãªtre rÃ©utilisÃ© Ã  chaque updated'Antigravity. CaractÃ©ristiques :- **Idempotent** : re-running sur un asar dÃ©jÃ  patchÃ© ne change rien  (delta = 0 B).- **Safety check** : refuse de packager si le delta de taille dÃ©passe  100 KB (signe qu'un overlay complet non intentionnel s'est produit).- **Doc embarquÃ©e** : le header explique la diffÃ©rence de version 2.0/2.1 vs  2.2.x et la lesson learned.- **3 Ã©tapes** : extract â†’ inject 3 modules â†’ repack.Usage aprÃ¨s un update d'Antigravity :```bash# Mettre Ã  jour le binaire via le mÃ©canisme standard de Google# Puis relancer le patch :NODE_PATH="$REPO/node_modules" node "$REPO/scripts/patch_2_2_1.js" \    "$RES/app.asar" "/tmp/ag-build" "/tmp/app.asar.patched"```---## 8. LeÃ§ons apprises### 8.1 Toujours lire les logs applicatifs AVANT l'erreur rÃ©seauL'erreur `127.0.0.1:443 refused` est dÃ©routante parce qu'elle parle rÃ©seau,mais la cause est dans `main.log` :```[error] [PATCH] startProxy failed: Cannot find module '../cryptoStore'```â†’ Le rÃ©flexe : `grep -i "error\|fail" main.log` **avant** de chercher du cÃ´tÃ©des ports/firewall/rÃ©seau.### 8.2 Diff `dist/` du repo vs `dist/` de l'asar dÃ©ployÃ©```bash# Fichiers dans dist/ du repo mais pas dans l'asar dÃ©ployÃ© :npx -y @electron/asar list "$RES/app.asar" | sort > /tmp/asar.txtls "$REPO/dist/"*.js | xargs -n1 basename | sort > /tmp/dist.txtcomm -23 /tmp/dist.txt <(awk -F'/' '/^\/dist\/[a-zA-Z][^\/]*\.js$/ {print $3}' /tmp/asar.txt | sort)```Cette commande a rÃ©vÃ©lÃ© les 3 modules manquants.### 8.3 L'overlay complet de `dist/` n'est PAS une option sÃ»reUn repack qui copie tout `dist/` :- Remplace le `dist/main.js` patchÃ© par le `main.js` upstream (perd  l'integration TLS bypass + `require('../proxy-runner')`).- Emporte les `__mocks__/*` (mocks vitest) â†’ Electron rÃ©sout ces mocks  comme s'ils Ã©taient les vrais modules `electron`, `electron-log`, etc.- Emporte les `*.test.js` â†’ pollution de l'asar de production.â†’ Toujours faire un overlay **chirurgical** : copier uniquement les fichiersexplicitement nÃ©cessaires.### 8.4 Backup avant chaque opÃ©ration destructiveLe pattern `app.asar.pre-<operation>-<ISO>.bak` a sauvÃ© la mise plusieurs fois :```app.asar.pre-recon-20260711T133348.bak       â† utilisÃ© pour restaurer aprÃ¨s tentative ratÃ©eapp.asar.pre-surgical-20260711T135228.bak    â† snapshot aprÃ¨s restauration```Ã€ garder : au minimum les 3 derniers backups.### 8.5 Versioning sÃ©mantique d'Antigravity = breaking changes| 2.0.x â†’ 2.1.0 | pas de breaking change (proxy intact) || 2.1.0 â†’ 2.2.x | **breaking** : proxy code supprimÃ© du bundle officiel || 2.2.x â†’ 2.3+ | Ã  surveiller (le pattern de Google est de rÃ©duire le surface area) |Ã€ chaque update, refaire le diagnostic de Â§3 avant d'appliquer le patch.---## 9. RÃ©fÃ©rences- **Session kimchi de rÃ©fÃ©rence** : `019f4c39-447f-74ac-8dc8-8e4be561aa0d` (2026-07-10 13:30)  - Message 14:02:01 : explication de la suppression du proxy en v2.2.x  - Message 14:20:24 : premiÃ¨re confirmation de patch rÃ©ussi- **Patcher** : `scripts/patch_2_2_1.js`- **Code source des modules injectÃ©s** :  - `src/cryptoStore.ts`  - `src/customModelStore.ts`  - `src/schemaValidator.ts`- **Caller** : `src/proxy/modelLoader.ts` (ligne 10 : `import * as cryptoStore from '../cryptoStore';`)- **Fallback dans le LS** : `src/languageServer.ts` (ligne 168-169)- **Autre doc liÃ©e** :  - `TROUBLESHOOTING.md` (flowchart port 50999)  - `SOLUTION_LAUNCH_FIX.md` (problÃ¨me de lancement aprÃ¨s update)  - `docs/MITM-Notes.md` (notes sur le MITM 443)